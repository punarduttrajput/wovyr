# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Env / secrets
.env
*.local

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local-only AI-assistant context
/CLAUDE.md
/CLAUDE.full.md

# Local-only MCP server config (personal connectors)
/.mcp.json

# Local-only working files. Anything under /internal/ is never published — put
# unpublished notes there rather than adding new patterns below. The remaining
# entries cover files that predate that convention.
/internal/
/DISTRIBUTION.md
/DesignPartner-*
/*.xlsx
/*.log
/website/landing/PRD-landing.md
/website/landing/TRD-landing.md

# DX-503: Lighthouse CI's local scratch output
.lighthouseci/
lhci-reports/
