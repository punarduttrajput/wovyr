# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Env / secrets
.env
*.local

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local-only AI-assistant context (kept out of the public repo)
/CLAUDE.md
/CLAUDE.full.md

# Local-only MCP server config (personal connectors — not for the public repo)
/.mcp.json

# Internal / business docs (design-partner, pentest, outreach — not for the public repo)
/DesignPartner-NCR.md
/DesignPartner-Outreach-NCR.xlsx
/Wovyr-Pentest-Report.xlsx

# DX-503: Lighthouse CI's local scratch output
.lighthouseci/
lhci-reports/
