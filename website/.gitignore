node_modules/
dist/
.astro/
# Generated from ../docs by scripts/sync-docs.mjs — never edit or commit.
src/content/docs/
# WEB-301: generated by scripts/generate-og-image.mjs (npm run og, part of the
# dev/build scripts) — never edit or commit, same as src/content/docs/ above.
public/og.png
# WEB-305: copied from ../demo/out by scripts/sync-demo.mjs (npm run demo, part
# of the dev/build scripts). The originals are committed under demo/out/ — a
# second copy here would just be one more thing to drift.
public/demo/
# GEO-101: generated by scripts/sync-docs.mjs alongside src/content/docs/ —
# derived from ../docs, so committing them would just be a second copy to drift.
public/llms.txt
public/llms-full.txt
# Scratch state written by the Netlify CLI on `netlify deploy` (and the site
# link, if one is ever created here) — machine-local, never committed.
.netlify/
