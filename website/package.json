{
  "name": "wovyr-docs-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "og": "node scripts/generate-og-image.mjs",
    "dev": "npm run sync && npm run og && astro dev",
    "build": "npm run sync && npm run og && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.35.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "astro": "^5.12.0",
    "sharp": "^0.34.0"
  }
}
