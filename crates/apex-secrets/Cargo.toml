[package]
name = "apex-secrets"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Secret vault: reference-addressed, tenant-scoped, grant-gated secret storage with masking and rotation — the single source of truth for credentials."

[dependencies]
apex-common.workspace = true
serde.workspace = true
serde_json.workspace = true
