[package]
name = "apex-secrets"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
description = "Secret vault: reference-addressed, tenant-scoped, grant-gated secret storage with masking and rotation — the single source of truth for credentials."

[dependencies]
apex-common.workspace = true
apex-kms.workspace = true
serde.workspace = true
serde_json.workspace = true
# For the loud plaintext-migration warning (SEC-101) — the operator must be told a
# plaintext backup file still exists after the one-time re-seal.
tracing.workspace = true
