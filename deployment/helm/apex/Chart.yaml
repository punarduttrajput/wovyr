apiVersion: v2
name: apex
description: >-
  Helm chart for the Apex AI Platform's real, single-binary v0.1 topology
  (one `apex` server + Postgres + Qdrant) — the same shape as
  deployment/docker-compose.yml, not the aspirational multi-service split
  described in docs/12-deployment/kubernetes.md and helm.md. See this
  chart's README.md for exactly what that means and what's still open.
type: application
version: 0.3.0
appVersion: "0.3.0"
keywords:
  - apex
  - agent-platform
home: https://github.com/punarduttrajput/Apex
sources:
  - https://github.com/punarduttrajput/Apex
