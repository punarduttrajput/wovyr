apiVersion: v1
kind: Service
metadata:
  name: {{ include "wovyr.fullname" . }}-postgres
  labels:
    {{- include "wovyr.labels" . | nindent 4 }}
    {{- include "wovyr.postgres.selectorLabels" . | nindent 4 }}
spec:
  # Headless: wovyr's DSN and the wait-for-postgres init container address
  # this StatefulSet's single pod by its stable per-pod DNS name, not a
  # load-balanced VIP.
  clusterIP: None
  ports:
    - name: postgres
      port: 5432
  selector:
    {{- include "wovyr.postgres.selectorLabels" . | nindent 4 }}
