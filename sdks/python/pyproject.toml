[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "apex-ai-sdk"
version = "0.1.0"
description = "Python client for the Apex AI Platform API."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/punarduttrajput/Apex"

[tool.hatch.build.targets.wheel]
packages = ["apex_sdk"]
