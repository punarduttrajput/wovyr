<!--
File: docs/18-roadmap/v1.2-generative-ui.md
Document ID: RM-GUI-001
-->

# v1.2 — Generative UI Trust Runtime

**Document ID:** RM-GUI-001
**File Path:** `docs/18-roadmap/v1.2-generative-ui.md`
**Version:** 1.0.0
**Status:** Planned — next milestone after v1.1 Phase 2
**Owner:** Product / Founder
**Last Updated:** 2026-07-14

---

# 1. Purpose

Execute [PRD-005](../01-product/prd-generative-ui-runtime.md) (decision:
[ADR-0011](../17-adr/ADR-0011-generative-ui-repositioning.md)): ship the frame
protocol, the trust/policy engine, the durable interaction loop, the renderer
SDK, and the internal-tools beachhead — in three strictly sequential phases with
measured exit criteria.

Requirement IDs (UIP/GRD/HIL/RDR/CMP/ITS/EMB) are defined in PRD-005 §6; tickets
here carry the `RM-GUI-P<phase>` prefix and reference them.

**Relationship to v1.1:** Phase 1/2 of v1.1 are done. v1.1 Phase 3
(ecosystem & scale) is **re-scoped through PRD-005**: items serving the trust
runtime (MCP surface, SDK distribution) fold into RM-GUI-P3; purely horizontal
items are deferred to [future.md](future.md).

---

# 2. Phase 1 — Protocol & Trust Core (server-side only)

**Theme:** frames exist and are governed. No renderer; acceptance is against the
API/SSE stream and the audit chain.

| Ticket | Requirement(s) | Summary |
|---|---|---|
| RM-GUI-P1-101 | UIP-101, UIP-106 | New crate `apex-ui`: `UiFrame` tree over the constrained component vocabulary, `schemars`-derived schemas, semver'd schema version, newer-than-understood rejection |
| RM-GUI-P1-102 | UIP-102, UIP-103 | Provenance metadata (run id, model id, prompt pin, canonical content hash) + declared intents on every actionable node; undeclared effects are validation errors |
| RM-GUI-P1-103 | UIP-104 | `UiFrame` event on `RunEventSink` → server SSE frame; workflow-activity output form; REST pull of the pending frame |
| RM-GUI-P1-104 | GRD-201, GRD-206 | New crate `apex-ui-guard`: `UiPolicy` YAML (forbidden component classes deny-by-default, approval-gate requirements, redaction, media-origin allow-list), tenant-scoped + versioned + pin-immutable |
| RM-GUI-P1-105 | GRD-202, GRD-207 | Guardrail-stage enforcement between generation and emission, fail-closed; hosted-run floor: no policy ⇒ input/action frames denied (`APEX_UNRESTRICTED_UI=1` escape hatch) |
| RM-GUI-P1-106 | GRD-203, GRD-204 | Intent-consistency structural checks + deception-shape rules (structural first; LLM-judge variant shadow-mode only) |
| RM-GUI-P1-107 | GRD-205 | Verdicts into the `apex-audit` hash chain (rule id + frame hash); blocked-frame content retained KMS-encrypted, policy-controlled |
| RM-GUI-P1-108 | HIL-301..303 | `ui` workflow activity (durable suspend on the `Interrupted` path); typed decision validation; `POST /api/v1/ui/decisions/{frame-id}` over the human-approval signal path, decision-taker recorded |
| RM-GUI-P1-109 | — | Integration tests: UC1 (block-then-allow) and UC4 (injection containment) end-to-end over SSE + audit `verify()`; crash-kill between frame and decision resumes clean (the existing startup-resume test pattern) |

**Exit:** UC1/UC4 green in CI, including the crash-recovery variant.

---

# 3. Phase 2 — Renderer & Interaction Loop

**Theme:** humans see and decide. DX-measured exit.

| Ticket | Requirement(s) | Summary |
|---|---|---|
| RM-GUI-P2-201 | RDR-401, RDR-403 | `@apex/ui-react`: SSE/pull consumption, vocabulary rendering with design tokens, frame-hash verification, inert visible placeholder for unknown nodes |
| RM-GUI-P2-202 | RDR-402 | Web-component build over the same core (Angular dashboard consumption) |
| RM-GUI-P2-203 | RDR-404 | Progressive composition rendering; guard-buffering supersedes streaming when an output UI guard is configured |
| RM-GUI-P2-204 | RDR-405 | Accessibility floor: WCAG-conformant vocabulary components by construction |
| RM-GUI-P2-205 | HIL-304..306 | `run_agent` present-frame/await-decision step; durable frame timers (expiry policy: fail/default/escalate); decision+frame-hash pair in the audit chain |
| RM-GUI-P2-206 | GRD-208 | Tenant validator hooks as `wasm32-wasi` modules in `WasiSandbox` (fuel/memory limits) |
| RM-GUI-P2-207 | CMP-501..503 | Signed UI templates (plugin signing path), marketplace verified-badge track extension, registry-pinned byte-identical rendering |
| RM-GUI-P2-208 | RDR-406 | SDK quickstart + docs; measured: fresh React app to first governed frame < 30 min |
| RM-GUI-P2-209 | PRD-005 §9 | The killer demo as an integration test + runnable example (`examples/ui/`): poisoned-source block, safe render, kill/restart, approve, `apex audit verify` |

**Exit:** killer demo runs from a fresh clone in <10 min; quickstart <30 min
measured with an external tester.

---

# 4. Phase 3 — Beachhead & Embeddability

**Theme:** sellable and adoptable.

| Ticket | Requirement(s) | Summary |
|---|---|---|
| RM-GUI-P3-301 | ITS-601, ITS-602 | Dashboard "Surfaces": intent → generated internal tool over Apex's own APIs; every action RBAC-checked server-side at decision time under the viewer's identity |
| RM-GUI-P3-302 | ITS-603, ITS-604 | Destructive-action auto-gating via the human-approval activity; saveable/shareable pinned template+policy surfaces |
| RM-GUI-P3-303 | EMB-701 | Standalone middleware mode: frames in over HTTP from any agent stack, verdicts/decisions out — one binary (ADR-0010 appliance stance) |
| RM-GUI-P3-304 | EMB-702 | MCP server surface: `ui_present` / `ui_await_decision` tools |
| RM-GUI-P3-305 | UIP-105, EMB-703 | Versioned interop mapping to/from A2UI-style component JSON and MCP Apps conventions, fail-closed on unsupported constructs; TS/Python SDK parity for emission + decisions |
| RM-GUI-P3-306 | EMB-704 | Public conformance suite (must-block / must-redact / must-pass vectors) wired into CI |
| RM-GUI-P3-307 | PRD-005 §8 | Design-partner program: onboarding docs, 5 partners, red-team exercise, ≥2 paid pilots |

**Exit:** Surfaces dogfooded on Apex's own ops; standalone + MCP modes
documented; 5 partners onboarded.

---

# 5. Cut Lines (solo-founder bandwidth, PRD-005 §10)

If a phase overruns, cut in this order — never the other:

- **P1:** LLM-judge consistency checks (GRD-203 judge variant) → structural rules
  only. Blocked-frame content retention (GRD-205 second half) → verdict-only audit.
- **P2:** Web-component build (RDR-402) → React only. Marketplace badge track
  (CMP-502) → signing only.
- **P3:** Interop mapping breadth (UIP-105) → one target shape first. Surfaces
  sharing (ITS-604) → per-operator only.

The fail-closed floors (GRD-202/207, HIL-302) and audit integration (GRD-205
verdicts, HIL-306) are **never** cut — they are the product.

---

# 6. Revision History

| Version | Date | Description |
|---------|------|-------------|
| 1.0.0 | 2026-07-14 | Initial v1.2 milestone: three phases executing PRD-005 |
